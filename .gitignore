.DS_Store
node_modules/
dist/
.env
.env.*

# Local artifact-tree state — created per-project at runtime
.agents/

# Per-project runtime state — context, experience, artifacts, loops, evals
# populated when skills run; not distributable plugin content
.forsvn/

# User-specific loops + experience substrate — not distributable plugin content
skills-resources/

# Local-only IDE / agent state
.scion/
.claude/settings.local.json
IDEA.md

# Internal development artifacts — not user-facing, not distributable
implementation-roadmap/
autoresearch/
