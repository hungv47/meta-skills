.DS_Store
node_modules/
dist/
.env
.env.*

# Per-project runtime state — context, experience, artifacts, loops, evals,
# and the machine-derived index (.forsvn/index/). Populated when skills run;
# not distributable plugin content.
.forsvn/

# Local-only IDE / agent state
.scion/
.claude/settings.local.json
IDEA.md

# Internal development artifacts — not user-facing, not distributable
implementation-roadmap/
autoresearch/
roughdraft/
references/internal/
references/private/
scripts/internal/
scripts/private/

# Internal release guideline — maintainer process, not distributed
/RELEASING.md

# Local-only push configuration — not for distribution
/PUSH.md
