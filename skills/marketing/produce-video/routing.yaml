# Routing signals for produce-video. Source of truth — compiled into hooks/skill-registry.json
# by `node hooks/build-registry.mjs`. NOT loaded into model context. Edit here, then rebuild.
promptSignals:
  phrases:
    - "produce video"
    - "render video"
    - "video manifest"
    - "video from brief"
    - "make the video"
    - "build the video"
    - "hyperframes scaffold"
    - "remotion scaffold"
    - "render-ready video"
    - "video export bundle"
    - "render the app preview"
    - "produce the app preview"
    - "build the app preview video"
    - "scaffold the app preview"
    - "render the app preview brief"
  allOf:
    - [produce, video]
    - [render, video, brief]
    - [video, manifest]
    - [render, app, preview]
    - [produce, app, preview]
  anyOf:
    - "produce-video"
    - "video render"
    - "hyperframes export"
    - "remotion export"
    - "video scaffold"
    - "app-preview bundle"
    - "app preview scaffold"
  noneOf:
    - "short-form brief"
    - "shoot brief"
    - "brief the app preview"
    - "write the app preview"
    - "compose the app preview"
    - "draft the app preview"
    - "publish"
    - "evaluate"
  minScore: 6
